/// Generated gRPC code for esphd daemon service (esphctl ↔ esphd).
pub mod daemon {
    tonic::include_proto!("esphd.v1");
}

// The cluster service messages are in the same protobuf package (esphd.v1),
// so they are included in the same module above.
