/// Worker services common to all nodes (both master and worker).
///
/// - Heartbeat reporting to master
/// - Local resource collection (CPU/Mem/Disk)
/// - FS mount state monitoring
pub struct WorkerServices {
    // TODO: heartbeat interval, resource collector, fs monitor
}
