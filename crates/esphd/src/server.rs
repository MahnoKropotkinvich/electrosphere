/// gRPC server implementation for esphd.
///
/// Listens on a Unix domain socket and serves the EsphDaemon service
/// defined in daemon.proto.

pub struct DaemonServer {
    // TODO: hold references to ContextManager, FSBackend
}
